{
  "name": "@create-game/template",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:itch": "tsc && vite build --outDir dist-itch --base ./",
    "preview": "vite preview",
    "balance": "tsx scripts/balance-sim.ts"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
