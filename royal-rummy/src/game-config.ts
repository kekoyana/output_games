export interface GameConfig {
  name: string;
  saveKey: string;
  accentColor: string;
}

export const GAME_CONFIG: GameConfig = {
  name: "Royal Rummy",
  saveKey: "royal-rummy-save",
  accentColor: "#c8a04a",
};
